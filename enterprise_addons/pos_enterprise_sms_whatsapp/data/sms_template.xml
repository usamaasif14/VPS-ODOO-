<?xml version="1.0" ?>
<odoo>
    <data noupdate="1">
            <record id="sms_template_order_received_confirmation_pos" model="sms.template">
            <field name="name">POS: Order received confirmation</field>
            <field name="model_id" ref="point_of_sale.model_pos_order"/>
            <field name="body">Hello {{ object.partner_id.name }},

Thank you for your order number {{ object.pos_reference }} for the {{ object.preset_time }}.
You will soon be notified when your order is ready.

{{ object.company_id.name }}

            </field>
        </record>

        <record id="sms_template_order_ready_pos" model="sms.template">
            <field name="name">POS: Order is ready</field>
            <field name="model_id" ref="point_of_sale.model_pos_order"/>
            <field name="body">Hello {{ object.partner_id.name }},

Your order number {{ object.pos_reference }} is now ready for pickup. Head over whenever you're ready, and enjoy your meal.

{{ object.company_id.name }}

            </field>
        </record>
    </data>
</odoo>
